<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Acceuil</title>

<style>
    body {
        background-color: #bd915a;
    }

    main {
        margin: 5rem;
    }

    .card-columns {
        max-width: 800px;
        margin: 0 auto;
    }

    .card:hover {
        transform: translateY(-0.5rem);
    }

</style>

    

</head>

<body>

<!--NAV-->
<header>

<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#">Accueil</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item active">
        <a class="nav-link" href="http://localhost:3000/">Accueil<span class="sr-only"></span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/team">Notre équipe</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/contact">Nous contacter</a>
      </li>
    </ul>
  </div>
</nav>

</header>

<!--MAIN-->

<main>

<h1>Bienvenue sur le site de potins !</h1>

<h2>Les derniers potins en date :</h2>


<!--<div class="card text-white bg-dark mb-3" style="max-width: 18rem;">
  <div class="card-header">Header</div>
  <div class="card-body">
    <h5 class="card-title">Dark card title</h5>
    <p class="card-text">Some quick example text to build on the card title and make up the bulk of the card's content.</p>
  </div>
</div>

<% @gossips.each do |gossip| %>
  <p><%= link_to gossip.title, gossip_path(gossip.id) %> - Par <%= gossip.author.first_name %></p>
<% end %>


  <div class="card-columns">
      <% @gossips.each do |gossip| %>
        <div class="card text-white bg-dark mb-3">
          <div class="card-header"><%= gossip.title %></div>
          <div class="card-body">
            <h5 class="card-title">Par <%= link_to gossip.author.first_name, user_path(gossip.author.id) %></h5>
            <p class="card-text"><%= gossip.content %></p>
            <p class="card-text"><small class="text-muted">Créé le <%= gossip.created_at.strftime('%d/%m/%Y à %H:%M') %></small></p>
          </div>
        </div>
      <% end %>-->


      <div class="row">
  <% @gossips.each do |gossip| %>
    <div class="col-md-4 mb-4">
      <div class="card text-white bg-dark">
        <div class="card-header"><%= gossip.title %></div>
        <div class="card-body">
          <h5 class="card-title"><%= gossip.author.first_name %></h5>
          <p class="card-text"><%= gossip.content %></p>
          <%= link_to "Voir le gossip", gossip_path(gossip.id), class: "btn btn-primary" %>
        </div>
      </div>
    </div>
  <% end %>
</div>


</main>

</body>

</html>
